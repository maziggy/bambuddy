# Claude Code entry point

Read [`AGENTS.md`](AGENTS.md) first — it's the harness-agnostic agent guide for this
repo and links to the full knowledge base in `docs/airtho/`. This file exists only
because Claude Code looks for `CLAUDE.md` specifically; keep it a thin pointer rather
than duplicating content, so the two can't drift out of sync.
